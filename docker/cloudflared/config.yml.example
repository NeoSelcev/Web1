# Cloudflare Tunnel Configuration для Home Assistant
# Этот файл нужно создать после регистрации туннеля через:
# cloudflared tunnel create homeassistant-tunnel

tunnel: [TUNNEL_ID_HERE]
credentials-file: /etc/cloudflared/[TUNNEL_ID_HERE].json

ingress:
  # Home Assistant Admin Panel (с Basic Auth + Cloudflare Access)
  - hostname: ha.[YOUR-DOMAIN]
    service: http://ha-proxy:8080
  
  # Тестовая веб-страница
  - hostname: test.[YOUR-DOMAIN]
    service: http://test-web:80
  
  # Catch-all правило (обязательно)
  - service: http_status:404
