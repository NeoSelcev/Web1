# Logrotate configuration for system-diagnostic service
# Place this file in /etc/logrotate.d/system-diagnostic

# Medium-frequency logs - moderate rotation
/var/log/system-diagnostic.log {
    # Rotate when file reaches 10MB OR weekly (whichever comes first)
    maxsize 10M
    
    # Keep 5 rotated files (50MB total per log)
    rotate 5
    
    # Compress old files
    compress
    delaycompress
    
    # Don't rotate empty files
    notifempty
    missingok
    
    # Create new file with correct permissions
    create 644 root root
    
    # Rotate weekly if size not reached
    weekly
}
